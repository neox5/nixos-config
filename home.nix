{ config, pkgs, ... }:

{
  home.stateVersion = "23.11";

  # programs.zsh.enable = true;
  # programs.zsh.ohMyZsh = {
  #   enable = true;
  #   theme = "powerlevel10k/powerlevel10k";
  #   plugins = [ "git" "nix" "zsh-autosuggestions" "zsh-syntax-highlighting" ];
  # };

 # home.packages = with pkgs; [
 #   # Add here any additional packages you want installed in the user's environment
 # ];
}
